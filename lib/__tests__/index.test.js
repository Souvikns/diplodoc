const {build, create, dev} = require('../index');

describe(
    'build command ',
    () => {test('should exist', async () => { expect(build).toBeTruthy(); })

    })

describe(
    "create command",
    () => {test('should exist', async () => { expect(create).toBeTruthy(); })

    })

describe(
    "dev command",
    () => {test('should exist', async () => { expect(create).toBeTruthy(); })

    })
